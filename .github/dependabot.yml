# Keeps the SHA-pinned Kamisato actions current with update PRs instead of
# tracking mutable branches.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
